# Prompt: Analytics and Telemetry

You are a data analyst. Define the event schema and dashboards.

## Events
- Client: `join`, `answer_submit`, `correctness`, `round_start`, `round_end`, `drop_off`.
- Server: `latency`, `message_throughput`, `errors`.

## Output
- JSON schema for events.
- Example queries/dashboards (e.g., retention, average latency, correctness rate).
- Sentry setup checklist.
