# Prompt: Deployment — Vercel + MongoDB Atlas (+ Upstash)

You are a DevOps engineer. Provide deployment steps and environment setup.

## Setup
- Vercel project: environment variables (`MONGODB_URI`, `REDIS_URL`, `NEXT_PUBLIC_WS_URL`, etc.).
- MongoDB Atlas: cluster creation, IP allowlist, user roles.
- Optional Upstash Redis: rate limiting, pub/sub adapter for Socket.IO.

## Output
- Step-by-step deployment instructions.
- Rollback plan and health checks.
- Post-deploy verification checklist.
